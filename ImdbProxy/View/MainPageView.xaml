<UserControl x:Class="ImdbProxy.View.MainPageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:v="clr-namespace:ImdbProxy.View"
             xmlns:vm="clr-namespace:ImdbProxy.View"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1000">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        
        <Button Content="Find Movie" Grid.Column="0" Grid.Row="0" Command="{Binding GetConcreteMovieAsyncCommand}"/>
        
        <ListView Grid.Column="0" Grid.Row="1" ItemsSource="{Binding MoviesList}" SelectedIndex="{Binding SelectedIndex}"/>
        
    </Grid>
</UserControl>
